ServerConfig = {}

ServerConfig.Webhook = {
    url    =
    'your_webhook_url',
    name   = 'RS Drug Sell',
    avatar = '',
    color  = 8073662,
}

ServerConfig.Logging = {
    sales        = true,
    fails        = false,
    policeAlerts = true,
    richEmbed    = true,
}
